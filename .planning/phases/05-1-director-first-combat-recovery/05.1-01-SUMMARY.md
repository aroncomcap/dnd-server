# Summary 05.1-01: Target Authority And Tactical Fast Path

## Completed

- Added shared target-authority helpers for preferred attack/support targets and legal target validation.
- Routed parser defaults through persistent player target preferences.
- Made combat-engine target-required actions fail closed before dice rolls, turn advancement, or spell-slot spending.
- Added deterministic tactical combat fast path for parseable combat actions, bypassing narration LLM calls for low-value tactical turns.
- Made enemy turns deterministic by default; the enemy tactics LLM path is now opt-in via `ENEMY_TACTICS_LLM=true`.
- Added compact combat target selectors for attack/support targets.
- Added host recovery flow: `Move to Next Beat`.
- Added optional `Finish Cinematically` proposal/confirmation flow.

## Verification

- `node --test --test-concurrency=1 tests/action-parser.test.js tests/combat-engine.test.js tests/game-ui-regression.test.js`
- `npm test`
- `git diff --check`

## Result

Local verification passed. Production deploy and browser campaign rerun remain the next validation gate.
