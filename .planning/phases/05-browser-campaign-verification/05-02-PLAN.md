# Plan 05-02: Deploy Stabilization Batch And Rerun Production Campaign

## Goal

Deploy the locally verified player-intent and actionable-feedback stabilization batch to production, confirm the live deploy stamp advances, and rerun the browser campaign gate against `theystillsing.com`.

## Work Breakdown

1. Commit the intentional source, test, and GSD planning changes from Plan 05-01.
2. Push `main` so Railway creates a new production deployment.
3. Confirm Railway reports a deployment newer than the stale May 13 deployment.
4. Check production health and deploy time.
5. Run the Chromium campaign gate against production.
6. Record whether the campaign reaches its target or produces a precise product failure.

## Verification

- `git status --short`
- `git log -1 --oneline`
- `git push`
- `railway deployment list`
- `railway logs --deployment --lines 160 <deployment-id>`
- `curl -I https://theystillsing.com/lobby`
- `curl -s https://theystillsing.com/api/deploy-time`
- `curl -s https://theystillsing.com/health`
- `npx playwright test tests/e2e/campaign-verbose.spec.ts --project=chromium`

## Expected Outcome

The deploy stamp advances and the campaign gate either passes or identifies the next actionable stabilization defect.
